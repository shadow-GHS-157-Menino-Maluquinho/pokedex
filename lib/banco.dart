import 'package:flutter/material.dart';
import 'package:hive/hive.dart';
import 'package:hive_flutter/hive_flutter.dart';
//import 'package:hive_flutter/adapters.dart';





class salvar_pokemom {
  var name;
  var ids;
  var image;
  var vida;
  var tipo;
  var altura;
  var peso;
  var atack;

   salvar_pokemom({ 
    required this.name,
    required this.ids,
    required this.image,
    required this.tipo,
    required this.vida,
    required this.atack,
    required this.altura,
    required this.peso,
   });
   Future <void> criar()async{
   
   var a = pegar_pokemom();
    List b;
     b = a.criar() as List;
    if (b.length >= 5){
       print("quantidade exedita de pokemons");
    }
    else{
  await Hive.initFlutter();  // inicia o hive 
  // abre ou cria  a tabela 

  var meusPoke = await Hive.openBox<Map>('meus_poke');

  //  adiciona e atualiza
  await meusPoke.put(name, {'ids': ids, 'name': name,'image':image,'tipo':tipo,'altura':altura,'peso':peso});


  print('Dados semeados com sucesso!');
  await meusPoke.clear();
  // 4. Fechar a caixa quando terminar (opcional, mas recomendado)
  await meusPoke.close();
    }
   }
   
}

class pegar_pokemom {
  

  Future <void> criar()async{

  await Hive.initFlutter();  // inicia o hive 


  var meusPoke = await Hive.openBox<Map>('meus_poke');

  //  adiciona e atualiza

   final pokemons = await meusPoke.values.toList();


  print(pokemons);

  
  await meusPoke.close();

   }
   





}























































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































/*import 'package:flutter/material.dart';
import 'package:sqflite/sqflite.dart';

class DB {
   DB._();
   static final DB instanc = DB._();
   static Database? _database;
   get database async {
    if (_database !=  null )return _database;
    return await _initDatabase();
   }
   _initDatabase() async{
    return await openDatabase(
      join(await getDatabasesPath(),"pokemons_dl"),
      version: 1,
      onCreate: _onCreate,

    );
   }
   _onCreate (db,version)async{
    await db.execute(_favorride);
    await db.execute(_pokemom_cp);
    await db.execute(_meuspoke);

    await db.insert('favorido',{"ids":[]});
   }
   String  get _favorride => '''
       CREATE TABLE favorido (
       id INTERGER PRIMARY KEY AUTOINCREMENT,
       ids LIST
      
       

       )
    
  ''';

String  get  _pokemom_cp => '''
       CREATE TABLE capiturados (
       id INTERGER PRIMARY KEY AUTOINCREMENT,
       ids TEXT,
       nome TEXT,
       foto  TEXT,
       vida  TEXT,
       forca  TEXT,
       peso  TEXT, 
       altura  TEXT,
       )
    
  ''';
String  get  _meuspoke => '''
       CREATE TABLE capiturados (
       id INTERGER PRIMARY KEY AUTOINCREMENT,
       ids TEXT,
       nome TEXT,
       foto  TEXT,
       vida  TEXT,
       forca  TEXT,
       peso  TEXT, 
       altura  TEXT,
       )
    
  ''';
}
class ContaRepository extends ChangeNotifier{
  late Database db;
  List <Posicao> _carteira =[]
  double _saldo = 0 ;
  get saldo =>_saldo 
}

class Posicao{
   Moeda moeda;
  double quantidade;
  Posicao({required this.moeda,required this.quantidade});

}*/